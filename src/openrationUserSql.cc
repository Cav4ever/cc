#include "openrationUserSql.h"
#include <iostream>

void OperationUserSql::insertUser(User user)
{
    std::cout<<"Inser a user in SQL." <<std::endl;
}

User *OperationUserSql::getUser(int id)
{
    std::cout<<"Get a user in SQL." <<std::endl;
    return NULL;
}